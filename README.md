qsjson is a microservice that returns the query string as a JSON object.

**usage**

- grab a binary from the releases page
- run it (`qsjson` or `qsjson -listenAddr localhost:8000`)
- `curl 'http://localhost:3000/?test=true&json=ok'`